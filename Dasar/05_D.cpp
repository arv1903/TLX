#include <bits/stdc++.h>
using namespace std;

int main() {

    int a;
    cin >> a;

    if ( a > 0 ) {
        cout << "positif" << endl;
    };

    if ( a == 0 ) {
        cout << "nol" << endl;
    };

    if ( a < 0 ) {
        cout << "negatif" << endl;
    };

    return 0;
}