#include <bits/stdc++.h>
using namespace std;

int main() {
    unsigned short a, b, c, d, e, f, g, h, i;

    cin >> a >> b >> c;
    cin >> d >> e >> f;
    cin >> g >> h >> i;

    cout << a  << " " << d << " " << g << "\n" << b << " " << e << " " << h << "\n" << c << " " << f << " " << i << "\n";


    return 0;
}